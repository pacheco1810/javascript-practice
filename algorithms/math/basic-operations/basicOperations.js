/**
 * Problem: Basic arithmetic operations
 * Topic: Math
 * Difficulty: Easy
 */

// Input: add(5, 3)
// Output: 8

const add = (a, b) => a + b;

// Input: rest(5, 3)
// Output: 2

const rest = (a, b) => a - b;

// Input: mul(5, 3)
// Output: 15

const mul = (a, b) => a * b;

// Input: div(10, 5)
// Output: 2

const div = (a, b) => a / b;

module.exports = {
    add,
    rest,
    mul,
    div
};